# Deploy

1. Run the build to check for errors
2. If build passes, commit all changes with a descriptive message
3. Push to the remote repository

```bash
npm run build && git add -A && git commit -m "Deploy: [describe changes]" && git push
```
